MiniCode's scheduled tasks have produced several state errors. Removing the last scheduled task from a workspace can leave a stale task on disk that reappears after restart. When completed-run history grows beyond 500 entries, a still-pending run or cleanup owner can disappear. Keep completed history bounded in memory and on disk while retaining unfinished runs and cleanup owners. A schedule in the machine's local timezone may shift by an hour across daylight-saving time, and expired or invalid persisted schedules must never fire.

Trace scheduler load, save, pruning, and tick behavior through the real source. Fix these related failures without weakening the existing tests. Add focused regression tests in new files, run relevant checks, and explain how the on-disk state and future fire times behave after your changes.
