# The Vite dev proxy discovers the MiniCode backend at startup.
# start.bat/start.sh can still override this with VITE_DEV_BACKEND_ORIGIN,
# VITE_API_BASE_URL, or VITE_WS_BASE_URL when they launch dynamic ports.
